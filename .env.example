# === OAuth (optional — enables "Sign in with GitHub" button) ===
# Without these, the app falls back to personal access token login.

# GitHub OAuth App client ID (public, safe for frontend)
VITE_GITHUB_CLIENT_ID=

# OAuth proxy URL (for GitHub Pages / static deploys)
# Deploy the worker in /worker/ to Cloudflare Workers and set the URL here.
# For Vercel deploys, leave blank — it uses /api/auth/callback instead.
VITE_OAUTH_PROXY_URL=

# GitHub OAuth App client secret (server-side only — never in frontend)
# Set this in Vercel env vars or Cloudflare Worker secrets, NOT here.
# OAUTH_CLIENT_SECRET=
